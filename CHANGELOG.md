# Changelog

All notable changes to StudySense AI are documented here.

## [Unreleased]

### Added
- Configurable `max_workers` via settings
- Configurable `log_level` via settings
- Configurable `gemini_temperature` via settings
- `/health` endpoint for service health checks
- `/version` endpoint for API version info
- `app_name` setting for deployment branding
